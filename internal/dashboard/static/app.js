(function () {
  'use strict';

  /* ---------- provider identity: official marks (Lobe Icons, MIT; logos belong to their owners) ---------- */

  function markAnthropic() {
    return '<g fill="currentColor" fill-rule="evenodd"><path d="M13.827 3.52h3.603L24 20h-3.603l-6.57-16.48zm-7.258 0h3.767L16.906 20h-3.674l-1.343-3.461H5.017l-1.344 3.46H0L6.57 3.522zm4.132 9.959L8.453 7.687 6.205 13.48H10.7z"/></g>';
  }

  function markOpenAI() {
    return '<g fill="currentColor" fill-rule="evenodd"><path d="M9.205 8.658v-2.26c0-.19.072-.333.238-.428l4.543-2.616c.619-.357 1.356-.523 2.117-.523 2.854 0 4.662 2.212 4.662 4.566 0 .167 0 .357-.024.547l-4.71-2.759a.797.797 0 00-.856 0l-5.97 3.473zm10.609 8.8V12.06c0-.333-.143-.57-.429-.737l-5.97-3.473 1.95-1.118a.433.433 0 01.476 0l4.543 2.617c1.309.76 2.189 2.378 2.189 3.948 0 1.808-1.07 3.473-2.76 4.163zM7.802 12.703l-1.95-1.142c-.167-.095-.239-.238-.239-.428V5.899c0-2.545 1.95-4.472 4.591-4.472 1 0 1.927.333 2.712.928L8.23 5.067c-.285.166-.428.404-.428.737v6.898zM12 15.128l-2.795-1.57v-3.33L12 8.658l2.795 1.57v3.33L12 15.128zm1.796 7.23c-1 0-1.927-.332-2.712-.927l4.686-2.712c.285-.166.428-.404.428-.737v-6.898l1.974 1.142c.167.095.238.238.238.428v5.233c0 2.545-1.974 4.472-4.614 4.472zm-5.637-5.303l-4.544-2.617c-1.308-.761-2.188-2.378-2.188-3.948A4.482 4.482 0 014.21 6.327v5.423c0 .333.143.571.428.738l5.947 3.449-1.95 1.118a.432.432 0 01-.476 0zm-.262 3.9c-2.688 0-4.662-2.021-4.662-4.519 0-.19.024-.38.047-.57l4.686 2.71c.286.167.571.167.856 0l5.97-3.448v2.26c0 .19-.07.333-.237.428l-4.543 2.616c-.619.357-1.356.523-2.117.523zm5.899 2.83a5.947 5.947 0 005.827-4.756C22.287 18.339 24 15.84 24 13.296c0-1.665-.713-3.282-1.998-4.448.119-.5.19-.999.19-1.498 0-3.401-2.759-5.947-5.946-5.947-.642 0-1.26.095-1.88.31A5.962 5.962 0 0010.205 0a5.947 5.947 0 00-5.827 4.757C1.713 5.447 0 7.945 0 10.49c0 1.666.713 3.283 1.998 4.448-.119.5-.19 1-.19 1.499 0 3.401 2.759 5.946 5.946 5.946.642 0 1.26-.095 1.88-.309a5.96 5.96 0 004.162 1.713z"/></g>';
  }

  function markXAI() {
    return '<g fill="currentColor" fill-rule="evenodd"><path d="M6.469 8.776L16.512 23h-4.464L2.005 8.776H6.47zm-.004 7.9l2.233 3.164L6.467 23H2l4.465-6.324zM22 2.582V23h-3.659V7.764L22 2.582zM22 1l-9.952 14.095-2.233-3.163L17.533 1H22z"/></g>';
  }

  function markKimi() {
    return '<path d="M21.846 0a1.923 1.923 0 110 3.846H20.15a.226.226 0 01-.227-.226V1.923C19.923.861 20.784 0 21.846 0z" fill="#1783FF"/><path d="M11.065 11.199l7.257-7.2c.137-.136.06-.41-.116-.41H14.3a.164.164 0 00-.117.051l-7.82 7.756c-.122.12-.302.013-.302-.179V3.82c0-.127-.083-.23-.185-.23H3.186c-.103 0-.186.103-.186.23V19.77c0 .128.083.23.186.23h2.69c.103 0 .186-.102.186-.23v-3.25c0-.069.025-.135.069-.178l2.424-2.406a.158.158 0 01.205-.023l6.484 4.772a7.677 7.677 0 003.453 1.283c.108.012.2-.095.2-.23v-3.06c0-.117-.07-.212-.164-.227a5.028 5.028 0 01-2.027-.807l-5.613-4.064c-.117-.078-.132-.279-.028-.381z" fill="#fff"/>';
  }

  function markOpenCode() {
    return '<g fill="currentColor" fill-rule="evenodd"><path d="M16 6H8v12h8V6zm4 16H4V2h16v20z"/></g>';
  }

  function markAntigravity() {
    return '<mask height="23" id="lobe-icons-antigravity-0-_R_0_" maskUnits="userSpaceOnUse" width="24" x="0" y="1"><path d="M21.751 22.607c1.34 1.005 3.35.335 1.508-1.508C17.73 15.74 18.904 1 12.037 1 5.17 1 6.342 15.74.815 21.1c-2.01 2.009.167 2.511 1.507 1.506 5.192-3.517 4.857-9.714 9.715-9.714 4.857 0 4.522 6.197 9.714 9.715z" fill="#fff"/></mask><g mask="url(#lobe-icons-antigravity-0-_R_0_)"><g filter="url(#lobe-icons-antigravity-1-_R_0_)"><path d="M-1.018-3.992c-.408 3.591 2.686 6.89 6.91 7.37 4.225.48 7.98-2.043 8.387-5.633.408-3.59-2.686-6.89-6.91-7.37-4.225-.479-7.98 2.043-8.387 5.633z" fill="#FFE432"/></g><g filter="url(#lobe-icons-antigravity-2-_R_0_)"><path d="M15.269 7.747c1.058 4.557 5.691 7.374 10.348 6.293 4.657-1.082 7.575-5.653 6.516-10.21-1.058-4.556-5.691-7.374-10.348-6.292-4.657 1.082-7.575 5.653-6.516 10.21z" fill="#FC413D"/></g><g filter="url(#lobe-icons-antigravity-3-_R_0_)"><path d="M-12.443 10.804c1.338 4.703 7.36 7.11 13.453 5.378 6.092-1.733 9.947-6.95 8.61-11.652C8.282-.173 2.26-2.58-3.833-.848-9.925.884-13.78 6.1-12.443 10.804z" fill="#00B95C"/></g><g filter="url(#lobe-icons-antigravity-4-_R_0_)"><path d="M-12.443 10.804c1.338 4.703 7.36 7.11 13.453 5.378 6.092-1.733 9.947-6.95 8.61-11.652C8.282-.173 2.26-2.58-3.833-.848-9.925.884-13.78 6.1-12.443 10.804z" fill="#00B95C"/></g><g filter="url(#lobe-icons-antigravity-5-_R_0_)"><path d="M-7.608 14.703c3.352 3.424 9.126 3.208 12.896-.483 3.77-3.69 4.108-9.459.756-12.883C2.69-2.087-3.083-1.871-6.853 1.82c-3.77 3.69-4.108 9.458-.755 12.883z" fill="#00B95C"/></g><g filter="url(#lobe-icons-antigravity-6-_R_0_)"><path d="M9.932 27.617c1.04 4.482 5.384 7.303 9.7 6.3 4.316-1.002 6.971-5.448 5.93-9.93-1.04-4.483-5.384-7.304-9.7-6.301-4.316 1.002-6.971 5.448-5.93 9.93z" fill="#3186FF"/></g><g filter="url(#lobe-icons-antigravity-7-_R_0_)"><path d="M2.572-8.185C.392-3.329 2.778 2.472 7.9 4.771c5.122 2.3 11.042.227 13.222-4.63 2.18-4.855-.205-10.656-5.327-12.955-5.122-2.3-11.042-.227-13.222 4.63z" fill="#FBBC04"/></g><g filter="url(#lobe-icons-antigravity-8-_R_0_)"><path d="M-3.267 38.686c-5.277-2.072 3.742-19.117 5.984-24.83 2.243-5.712 8.34-8.664 13.616-6.592 5.278 2.071 11.533 13.482 9.29 19.195-2.242 5.713-23.613 14.298-28.89 12.227z" fill="#3186FF"/></g><g filter="url(#lobe-icons-antigravity-9-_R_0_)"><path d="M28.71 17.471c-1.413 1.649-5.1.808-8.236-1.878-3.135-2.687-4.531-6.201-3.118-7.85 1.412-1.649 5.1-.808 8.235 1.878s4.532 6.2 3.119 7.85z" fill="#749BFF"/></g><g filter="url(#lobe-icons-antigravity-10-_R_0_)"><path d="M18.163 9.077c5.81 3.93 12.502 4.19 14.946.577 2.443-3.612-.287-9.727-6.098-13.658-5.81-3.931-12.502-4.19-14.946-.577-2.443 3.612.287 9.727 6.098 13.658z" fill="#FC413D"/></g><g filter="url(#lobe-icons-antigravity-11-_R_0_)"><path d="M-.915 2.684c-1.44 3.473-.97 6.967 1.05 7.804 2.02.837 4.824-1.3 6.264-4.772 1.44-3.473.97-6.967-1.05-7.804-2.02-.837-4.824 1.3-6.264 4.772z" fill="#FFEE48"/></g></g><defs><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="17.587" id="lobe-icons-antigravity-1-_R_0_" width="19.838" x="-3.288" y="-11.917"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="1.117"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="38.565" id="lobe-icons-antigravity-2-_R_0_" width="38.9" x="4.251" y="-13.493"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="5.4"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="36.517" id="lobe-icons-antigravity-3-_R_0_" width="40.955" x="-21.889" y="-10.592"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="4.591"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="36.517" id="lobe-icons-antigravity-4-_R_0_" width="40.955" x="-21.889" y="-10.592"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="4.591"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="36.595" id="lobe-icons-antigravity-5-_R_0_" width="36.632" x="-19.099" y="-10.278"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="4.591"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="34.087" id="lobe-icons-antigravity-6-_R_0_" width="33.533" x=".981" y="8.758"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="4.363"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="35.276" id="lobe-icons-antigravity-7-_R_0_" width="35.978" x="-6.143" y="-21.659"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="3.954"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="46.523" id="lobe-icons-antigravity-8-_R_0_" width="45.114" x="-11.96" y="-.46"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="3.531"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="24.054" id="lobe-icons-antigravity-9-_R_0_" width="25.094" x="10.485" y=".58"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="3.159"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="30.007" id="lobe-icons-antigravity-10-_R_0_" width="33.508" x="5.833" y="-12.467"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="2.669"></feGaussianBlur></filter><filter color-interpolation-filters="sRGB" filterUnits="userSpaceOnUse" height="26.151" id="lobe-icons-antigravity-11-_R_0_" width="22.194" x="-8.355" y="-8.876"><feFlood flood-opacity="0" result="BackgroundImageFix"></feFlood><feBlend in="SourceGraphic" in2="BackgroundImageFix" result="shape"></feBlend><feGaussianBlur result="effect1_foregroundBlur_977_115" stdDeviation="3.303"></feGaussianBlur></filter></defs>';
  }

  function markGeneric() {
    return '<g><circle cx="12" cy="12" r="8.4" fill="none" stroke="currentColor" stroke-width="1.5" opacity=".8"/>' +
      '<circle cx="12" cy="12" r="2.5" fill="currentColor"/></g>';
  }

  /* Each provider's colour runs through its mark and its bars. xAI and OpenCode are
     black-and-white brands, so they get an inverted white tile instead of a hue. */
  const PROVIDERS = {
    'anthropic': { name: 'Anthropic', brand: '#d97757', rgb: '217, 119, 87', mark: markAnthropic },
    'openai-codex': { name: 'Codex', brand: '#10a37f', rgb: '16, 163, 127', mark: markOpenAI },
    'xai-oauth': { name: 'xAI', brand: '#ecebe5', rgb: '236, 235, 229', mark: markXAI, tile: 'inverted' },
    'kimi-code': { name: 'Kimi', brand: '#1783ff', rgb: '23, 131, 255', mark: markKimi },
    'opencode-go': { name: 'OpenCode Go', brand: '#ecebe5', rgb: '236, 235, 229', mark: markOpenCode, tile: 'inverted' },
    'google-antigravity': { name: 'Antigravity', brand: '#3186ff', rgb: '49, 134, 255', mark: markAntigravity },
  };
  const FALLBACK = { brand: '#b6b3aa', rgb: '182, 179, 170', mark: markGeneric };

  /* ---------- palette ---------- */

  const C = {
    cyan: 'oklch(0.82 0.12 165)',
    green: 'oklch(0.8 0.13 150)',
    amber: 'oklch(0.84 0.13 80)',
    red: 'oklch(0.72 0.15 30)',
    violet: 'oklch(0.78 0.1 310)',
    dim: 'oklch(0.55 0.02 80)',
    blue: 'oklch(0.82 0.09 235)'
  };

  const STATE_COLOR = {
    STARTING: C.dim,
    RUNNING: C.blue,
    DONE: C.green,
    TIMEOUT: C.amber,
    STOPPED: C.dim,
    FAILED: C.red,
    QUOTA: C.red
  };
  const TERMINAL_STATES = ['DONE', 'TIMEOUT', 'STOPPED', 'FAILED', 'QUOTA'];

  const FLAG_COLOR = {
    'NO-TURN': C.amber,
    'NO-TOOLS': C.amber,
    'STALLED': C.red,
    'LOOPING': C.violet
  };
  const METER_BLUE = 'oklch(0.55 0.09 235)';

  const TOOL_COLOR = {
    read: C.cyan,
    search: C.cyan,
    fetch: C.cyan,
    edit: C.amber,
    execute: C.green,
    think: C.violet
  };

  const THINK_COLOR = {
    max: 'oklch(0.82 0.09 235)',
    xhigh: 'oklch(0.82 0.09 235)',
    high: 'oklch(0.75 0.09 235)',
    medium: 'oklch(0.62 0.004 260)',
    low: 'oklch(0.5 0.004 260)'
  };

  /* One colour per model, by its index in the server's model list. That list is
     sorted, so a model keeps its colour from poll to poll. */
  const MODEL_COLOR = [
    'oklch(0.8 0.13 150)',
    'oklch(0.85 0.13 60)',
    'oklch(0.78 0.13 290)',
    'oklch(0.82 0.09 235)',
    'oklch(0.78 0.13 20)',
    'oklch(0.83 0.12 195)',
    'oklch(0.78 0.13 330)',
    'oklch(0.86 0.14 110)'
  ];

  const MAX_ROWS = 14;
  const POLL_MS = 3000;
  const WINDOW_MS = 60 * 60 * 1000;
  /* A tool call shorter than this is not worth a duration next to it. */
  const SHOW_SINCE_SEC = 5;

  /* ---------- small helpers ---------- */

  const pad2 = (n) => (n < 10 ? '0' + n : String(n));

  const num = (v) => {
    const n = Number(v);
    return Number.isFinite(n) ? n : 0;
  };

  const fmtK = (n) => (n >= 1e9 ? (n / 1e9).toFixed(2) + 'B' : n >= 1e6 ? (n / 1e6).toFixed(2) + 'M' : n >= 1e3 ? Math.round(n / 1e3) + 'k' : String(Math.round(n)));

  const mmss = (sec) => pad2(Math.floor(sec / 60)) + ':' + pad2(Math.floor(sec % 60));

  const money = (v) => '$' + num(v).toFixed(2);

  const parseTime = (s) => {
    if (!s) return null;
    const t = Date.parse(s);
    return Number.isFinite(t) ? t : null;
  };

  const numbers = (v) => (Array.isArray(v) ? v.map(num) : []);

  const el = (tag, cls, text) => {
    const node = document.createElement(tag);
    if (cls) node.className = cls;
    if (text !== undefined) node.textContent = text;
    return node;
  };

  const clear = (node) => {
    while (node.firstChild) node.removeChild(node.firstChild);
  };

  const providerKey = (model) => {
    const s = String(model || '');
    const i = s.indexOf('/');
    return i < 0 ? '' : s.slice(0, i);
  };

  const modelName = (model) => {
    const s = String(model || '');
    const i = s.indexOf('/');
    return i < 0 ? s : s.slice(i + 1);
  };

  /* Brand colour, tile background and mark for the provider that runs a model.
     Providers we do not know get a three-letter tile instead of a mark. */
  const identity = (model) => {
    const key = providerKey(model);
    const p = PROVIDERS[key];
    if (p) {
      const inverted = p.tile === 'inverted';
      return {
        name: p.name,
        brand: p.brand,
        mark: p.mark,
        short: '',
        bg: inverted ? 'rgba(236, 235, 229, 0.92)' : 'rgba(' + p.rgb + ', 0.16)',
        fg: inverted ? '#161615' : p.brand
      };
    }
    return {
      name: key || '—',
      brand: FALLBACK.brand,
      mark: key ? null : FALLBACK.mark,
      short: key ? key.toUpperCase().slice(0, 3) : '—',
      bg: 'rgba(' + FALLBACK.rgb + ', 0.16)',
      fg: FALLBACK.brand
    };
  };

  const stateOf = (w) => String(w.state || '').toUpperCase();
  const flagOf = (w) => String(w.flag || '').toUpperCase();
  const isTerminal = (state) => TERMINAL_STATES.indexOf(state) >= 0;

  const modeOf = (w) => (w.bare ? 'bare' : w.writes ? 'writes' : 'read-only');

  const uptimeText = (started, nowMs) => {
    const t = parseTime(started);
    if (t === null) return '—';
    const ms = Math.max(0, nowMs - t);
    const days = Math.floor(ms / 86400000);
    const hours = Math.floor((ms % 86400000) / 3600000);
    const mins = Math.floor((ms % 3600000) / 60000);
    if (days > 0) return days + 'd ' + pad2(hours) + 'h';
    if (hours > 0) return hours + 'h ' + pad2(mins) + 'm';
    return mins + 'm';
  };

  /* ---------- state ---------- */

  const dom = {
    canvas: document.getElementById('canvas'),
    uptime: document.getElementById('uptime'),
    running: document.getElementById('stat-running'),
    flagged: document.getElementById('stat-flagged'),
    spent: document.getElementById('stat-spent'),
    clock: document.getElementById('clock'),
    rows: document.getElementById('rows'),
    overflow: document.getElementById('overflow'),
    concNow: document.getElementById('conc-now'),
    concPeak: document.getElementById('conc-peak'),
    concArea: document.getElementById('conc-area'),
    concLine: document.getElementById('conc-line'),
    tokNow: document.getElementById('tok-now'),
    tokBars: document.getElementById('tok-bars'),
    tokLegend: document.getElementById('tok-legend'),
    spendNow: document.getElementById('spend-now'),
    spendBars: document.getElementById('spend-bars'),
    spendLegend: document.getElementById('spend-legend'),
    callsNow: document.getElementById('calls-now'),
    callsArea: document.getElementById('calls-area'),
    callsLine: document.getElementById('calls-line'),
    callsMarks: document.getElementById('calls-marks')
  };

  let data = null; /* last payload that arrived intact */
  let workerSig = ''; /* lets the tick skip a rebuild while the data is unchanged */
  let tracks = []; /* per-row handles the one-second tick writes to */
  let skewMs = 0; /* daemon clock minus browser clock */
  let request = null;
  let pollTimer = 0;

  /* A flag mark is drawn when a worker first raises a flag, or swaps one flag
     for another. The daemon reports the current flag only, so the times are
     remembered here between polls. */
  const flagSeen = new Map();
  let flagMarks = [];

  /* ---------- scaling ---------- */

  const fit = () => {
    const w = window.innerWidth;
    const h = window.innerHeight;
    if (w > 0 && h > 0) dom.canvas.style.setProperty('--scale', String(Math.min(w / 1920, h / 1080)));
    else requestAnimationFrame(fit);
  };

  /* ---------- rows ---------- */

  const elapsedSec = (w, nowMs) => {
    const start = parseTime(w.turn_started);
    if (start === null) return 0;
    const end = w.ended ? parseTime(w.ended) : nowMs;
    return Math.max(0, Math.round(((end === null ? nowMs : end) - start) / 1000));
  };

  const meterColor = (flag, budgetPct) => {
    if (flag && FLAG_COLOR[flag]) return FLAG_COLOR[flag];
    return budgetPct > 85 ? C.amber : METER_BLUE;
  };

  /* Elapsed time advances between polls, so the meter and its clock are
     repainted every second rather than waiting for the daemon. */
  const paintTrack = (t, nowMs) => {
    const sec = elapsedSec(t.worker, nowMs);
    const budgetPct = t.budget > 0 ? Math.min(100, (sec / 60 / t.budget) * 100) : 0;
    t.fill.style.setProperty('--elapsed-x', (t.starting ? 0 : budgetPct).toFixed(2) + '%');
    t.fill.style.setProperty('--fill', meterColor(t.flag, budgetPct));
    t.time.textContent = mmss(sec) + ' / ' + (t.budget > 0 ? t.budget + ':00' : '—');
  };

  const buildTile = (ident, logoH) => {
    const tile = el('div', 'tile');
    tile.style.setProperty('--tile-bg', ident.bg);
    tile.style.setProperty('--tile-fg', ident.fg);
    tile.style.setProperty('--logo-h', logoH + 'px');
    if (ident.mark) {
      const mark = el('span', 'mark');
      /* The marks are fixed SVG source shipped with the page, never data. */
      mark.innerHTML = '<svg viewBox="0 0 24 24" aria-hidden="true">' + ident.mark() + '</svg>';
      tile.appendChild(mark);
    } else {
      tile.appendChild(el('span', 'tile-text', ident.short));
    }
    return tile;
  };

  const buildWorkerCell = (w, layout) => {
    const cell = el('div', 'cell');
    const line = el('div', 'label-line');
    line.appendChild(el('span', 'label', String(w.label || w.id || '')));

    const state = stateOf(w);
    const flag = flagOf(w);
    /* A clean finish fades the row; any other ending, or a flag, is called out. */
    if (isTerminal(state) && state !== 'DONE') {
      const badge = el('span', 'badge', state);
      badge.style.setProperty('--badge', STATE_COLOR[state] || C.dim);
      line.appendChild(badge);
    } else if (flag) {
      const badge = el('span', 'badge', flag);
      badge.style.setProperty('--badge', FLAG_COLOR[flag] || C.amber);
      line.appendChild(badge);
    }

    /* Where the worker is working: its repository, or its agent when it has
       no tree. */
    const project = String(w.project || (w.bare ? w.agent || 'bare' : ''));

    if (layout.dense) {
      const inline = el('span', 'meta-inline');
      if (project) inline.appendChild(document.createTextNode(project + ' · '));
      inline.appendChild(el('span', 'worker-id', String(w.id || '')));
      line.appendChild(inline);
    }
    cell.appendChild(line);

    if (!layout.dense) {
      const meta = el('div', 'meta');
      if (project) {
        meta.appendChild(el('span', 'project', project));
        meta.appendChild(document.createTextNode(' · '));
      }
      meta.appendChild(el('span', 'worker-id', String(w.id || '')));
      meta.appendChild(document.createTextNode(' · ' + modeOf(w)));
      cell.appendChild(meta);
    }
    return cell;
  };

  const buildModelCell = (w, ident, layout) => {
    const cell = el('div', 'cell');
    const line = el('div', 'model-line');
    const name = el('span', 'model-name', modelName(w.model));
    name.style.setProperty('--brand', ident.brand);
    line.appendChild(name);

    const thinking = String(w.thinking || '');
    if (thinking) {
      const badge = el('span', 'think', thinking.toUpperCase());
      badge.style.setProperty('--think', THINK_COLOR[thinking.toLowerCase()] || 'oklch(0.6 0.004 260)');
      line.appendChild(badge);
    }
    cell.appendChild(line);

    if (!layout.dense) {
      const provider = el('span', 'provider-name', ident.name);
      provider.style.setProperty('--brand', ident.brand);
      cell.appendChild(provider);
    }
    return cell;
  };

  const toolBadge = (kind) => {
    const badge = el('span', 'tool', kind);
    badge.style.setProperty('--tool', TOOL_COLOR[kind.toLowerCase()] || C.dim);
    return badge;
  };

  const sinceText = (sec) => (sec >= 60 ? Math.floor(sec / 60) + 'm' + pad2(sec % 60) + 's' : sec + 's');

  /* A tool call as the agent described it, plus the file, command or
     pattern it named when the description does not already say so. */
  const appendCall = (line, w) => {
    const tool = String(w.tool || '');
    const title = String(w.title || '');
    const target = String(w.target || '');
    line.appendChild(toolBadge(tool));
    line.appendChild(el('span', 'act-text', title || target));
    if (target && title && title.indexOf(target) < 0) {
      line.appendChild(el('span', 'act-target', target));
    }
  };

  /* What the worker is doing right now, in one line: the tool call in
     flight; else the latest sentence it has written, reasoning or reply;
     else, once it has finished, the first line of what it said. Under it, a
     hairline for the turn's elapsed time against its budget. */
  const buildActivity = (w) => {
    const state = stateOf(w);
    const flag = flagOf(w);
    const budget = num(w.budget_min);
    const since = num(w.since_sec);

    const cell = el('div', 'act');
    const line = el('div', 'act-line');

    if (state === 'STARTING') {
      line.appendChild(el('span', 'act-text is-quiet', 'starting'));
    } else if (isTerminal(state)) {
      line.appendChild(el('span', 'act-text is-final', String(w.line || state.toLowerCase())));
    } else if (w.tool_live && w.tool) {
      appendCall(line, w);
      if (since >= SHOW_SINCE_SEC) line.appendChild(el('span', 'act-since', '· ' + sinceText(since)));
    } else if (w.said) {
      line.appendChild(el('span', 'act-said', String(w.said)));
      if (since >= SHOW_SINCE_SEC) line.appendChild(el('span', 'act-since', '· ' + sinceText(since)));
    } else if (w.tool) {
      line.appendChild(el('span', 'act-text is-quiet', 'after'));
      appendCall(line, w);
      if (since >= SHOW_SINCE_SEC) line.appendChild(el('span', 'act-since', '· ' + sinceText(since)));
    } else {
      line.appendChild(el('span', 'act-text is-quiet', 'thinking'));
      line.appendChild(el('span', 'act-since', '· ' + sinceText(since)));
    }
    cell.appendChild(line);

    const meterRow = el('div', 'act-meter');
    const meter = el('div', 'meter');
    const fill = el('div', 'meter-fill');
    meter.appendChild(fill);
    meterRow.appendChild(meter);
    const time = el('span', 'act-time');
    meterRow.appendChild(time);
    cell.appendChild(meterRow);

    return {
      node: cell,
      handle: {
        worker: w,
        fill: fill,
        time: time,
        budget: budget,
        flag: flag,
        starting: state === 'STARTING',
        live: !isTerminal(state) && !w.ended
      }
    };
  };

  const buildNumbers = (w) => {
    const cell = el('div', 'row-nums');

    const size = num(w.context_size);
    const pct = size > 0 ? Math.round((num(w.context_used) / size) * 100) : 0;
    const ctxColor = pct > 75 ? C.amber : 'oklch(0.85 0.004 260)';
    const ctx = el('div', 'ctx');
    ctx.style.setProperty('--ctx', ctxColor);
    ctx.appendChild(el('span', 'ctx-value', size > 0 ? pct + '%' : '—'));
    const bar = el('div', 'ctx-bar');
    const barFill = el('div', 'ctx-bar-fill');
    barFill.style.setProperty('--ctx-x', Math.max(0, Math.min(100, pct)) + '%');
    bar.appendChild(barFill);
    ctx.appendChild(bar);
    cell.appendChild(ctx);

    const tokens = num(w.tokens);
    cell.appendChild(el('span', null, tokens > 0 ? fmtK(tokens) : '—'));
    cell.appendChild(el('span', null, money(w.cost_usd)));
    return cell;
  };

  const buildRow = (w, layout) => {
    const state = stateOf(w);
    const flag = flagOf(w);
    const ident = identity(w.model);

    const row = el('div', 'row');
    row.style.setProperty('--state', (flag && FLAG_COLOR[flag]) || STATE_COLOR[state] || C.dim);
    row.classList.toggle('is-done', state === 'DONE');
    row.appendChild(buildTile(ident, layout.logoH));
    row.appendChild(buildWorkerCell(w, layout));
    row.appendChild(buildModelCell(w, ident, layout));

    const activity = buildActivity(w);
    row.appendChild(activity.node);
    row.appendChild(buildNumbers(w));

    return { node: row, track: activity.handle };
  };

  const renderRows = (workers) => {
    const shown = workers.slice(0, MAX_ROWS);
    const n = shown.length;
    const overflow = workers.length - shown.length;

    /* Rows share out the height left over: tighter and shorter as they pile up. */
    const dense = n > 10;
    const gap = dense ? 6 : 10;
    const avail = overflow > 0 ? 664 : 690;
    const rowH = n > 0 ? Math.min(62, Math.floor((avail - (n - 1) * gap) / n)) : 62;
    const layout = {
      dense: dense,
      logoH: Math.min(48, rowH - 8)
    };

    dom.rows.style.setProperty('--row-h', rowH + 'px');
    dom.rows.style.setProperty('--row-gap', gap + 'px');
    dom.rows.style.setProperty('--label-size', (dense ? 15 : 16) + 'px');
    dom.rows.style.setProperty('--model-size', (dense ? 15 : 16) + 'px');
    dom.rows.style.setProperty('--num-size', (dense ? 16 : 17) + 'px');
    dom.rows.style.setProperty('--act-size', (dense ? 14 : 15) + 'px');

    clear(dom.rows);
    tracks = [];

    if (n === 0) {
      dom.rows.classList.add('is-empty');
      dom.rows.appendChild(el('div', 'empty', 'no workers — fleet spawn'));
    } else {
      dom.rows.classList.remove('is-empty');
      const frag = document.createDocumentFragment();
      shown.forEach((w) => {
        const row = buildRow(w, layout);
        frag.appendChild(row.node);
        tracks.push(row.track);
      });
      dom.rows.appendChild(frag);
    }

    dom.overflow.hidden = overflow <= 0;
    dom.overflow.textContent = overflow > 0 ? '+' + overflow + ' more — fleet ls' : '';

    const now = Date.now() + skewMs;
    tracks.forEach((t) => paintTrack(t, now));
  };

  /* ---------- charts ---------- */

  /* Points for a 0..100 viewBox, leaving 8% of headroom above the peak. */
  const linePoints = (values, max) => {
    const n = values.length;
    if (n === 0) return '';
    const y = (v) => (max > 0 ? 100 - (v / max) * 92 : 100).toFixed(1);
    if (n === 1) return '0.0,' + y(values[0]) + ' 100.0,' + y(values[0]);
    return values.map((v, i) => ((i / (n - 1)) * 100).toFixed(1) + ',' + y(v)).join(' ');
  };

  const areaPoints = (points) => (points ? '0,100 ' + points + ' 100,100' : '');

  const rowTotals = (bins) => bins.map((bin) => numbers(bin).reduce((a, b) => a + b, 0));

  const renderBars = (host, bins, totals) => {
    clear(host);
    let max = 0;
    totals.forEach((t) => {
      if (t > max) max = t;
    });
    const frag = document.createDocumentFragment();
    bins.forEach((bin, i) => {
      const total = totals[i];
      const bar = el('div', 'bar');
      bar.style.setProperty('--h', (max > 0 ? Math.round((total / max) * 100) : 0) + '%');
      if (total > 0) {
        numbers(bin).forEach((v, j) => {
          if (v <= 0) return;
          const seg = el('div', 'seg');
          seg.style.setProperty('--h', Math.round((v / total) * 100) + '%');
          seg.style.setProperty('--seg', MODEL_COLOR[j % MODEL_COLOR.length]);
          bar.appendChild(seg);
        });
      }
      frag.appendChild(bar);
    });
    host.appendChild(frag);
  };

  const renderLegend = (host, models, values) => {
    clear(host);
    const frag = document.createDocumentFragment();
    models.forEach((model, j) => {
      const item = el('span', 'legend-item');
      const swatch = el('span', 'swatch');
      swatch.style.setProperty('--seg', MODEL_COLOR[j % MODEL_COLOR.length]);
      item.appendChild(swatch);
      item.appendChild(el('span', 'legend-name', modelName(model)));
      if (values) item.appendChild(el('span', 'legend-value', money(values[j])));
      frag.appendChild(item);
    });
    host.appendChild(frag);
  };

  /* Marks sit at the time the flag went up, on the same 60-minute window the
     series covers. */
  const renderFlagMarks = (series, binCount) => {
    clear(dom.callsMarks);
    const nowBrowser = Date.now();
    const bins = binCount > 0 ? binCount : 60;
    const binSec = num(series.hour_bin_sec) > 0 ? num(series.hour_bin_sec) : 60;
    const span = bins * binSec * 1000;
    const end = parseTime(series.end);
    const endBrowser = end === null ? nowBrowser : end - skewMs;
    const start = endBrowser - span;
    const frag = document.createDocumentFragment();
    flagMarks.forEach((m) => {
      const x = ((m.t - start) / span) * 100;
      if (x < 0 || x > 100) return;
      const mark = el('div', 'flag-mark');
      mark.style.setProperty('--x', x.toFixed(2) + '%');
      mark.style.setProperty('--mark', FLAG_COLOR[m.flag] || C.amber);
      frag.appendChild(mark);
    });
    dom.callsMarks.appendChild(frag);
  };

  const renderCharts = () => {
    const series = (data && data.series) || {};
    const models = Array.isArray(series.models) ? series.models : [];

    const conc = numbers(series.concurrency);
    let peak = 0;
    conc.forEach((v) => {
      if (v > peak) peak = v;
    });
    const concPoints = linePoints(conc, peak);
    dom.concNow.textContent = String(conc.length ? conc[conc.length - 1] : 0);
    dom.concPeak.textContent = 'peak ' + peak;
    dom.concLine.setAttribute('points', concPoints);
    dom.concArea.setAttribute('points', areaPoints(concPoints));

    const tokenBins = Array.isArray(series.tokens) ? series.tokens : [];
    const tokenTotals = rowTotals(tokenBins);
    dom.tokNow.textContent = fmtK(tokenTotals.reduce((a, b) => a + b, 0));
    renderBars(dom.tokBars, tokenBins, tokenTotals);
    renderLegend(dom.tokLegend, models, null);

    const spendBins = Array.isArray(series.spend) ? series.spend : [];
    const spendTotals = rowTotals(spendBins);
    const perModel = models.map((_, j) => spendBins.reduce((a, bin) => a + num(Array.isArray(bin) ? bin[j] : 0), 0));
    const spent = money(spendTotals.reduce((a, b) => a + b, 0));
    dom.spendNow.textContent = spent;
    dom.spent.textContent = spent;
    renderBars(dom.spendBars, spendBins, spendTotals);
    renderLegend(dom.spendLegend, models, perModel);

    const calls = numbers(series.calls);
    let callsMax = 0;
    calls.forEach((v) => {
      if (v > callsMax) callsMax = v;
    });
    const callsPoints = linePoints(calls, callsMax);
    dom.callsNow.textContent = String(num(series.calls_now));
    dom.callsLine.setAttribute('points', callsPoints);
    dom.callsArea.setAttribute('points', areaPoints(callsPoints));
    renderFlagMarks(series, calls.length);
  };

  /* ---------- header, tick, polling ---------- */

  const renderHeader = (workers) => {
    let running = 0;
    let flagged = 0;
    workers.forEach((w) => {
      if (!isTerminal(stateOf(w))) running++;
      if (flagOf(w)) flagged++;
    });
    dom.running.textContent = String(running);
    dom.flagged.textContent = String(flagged);
    dom.flagged.classList.toggle('raised', flagged > 0);
    dom.uptime.textContent = 'daemon up ' + uptimeText(data.started, Date.now() + skewMs);
  };

  const setUnreachable = (down) => {
    dom.uptime.classList.toggle('offline', down);
    if (down) dom.uptime.textContent = 'daemon unreachable';
  };

  const render = () => {
    const workers = Array.isArray(data.workers) ? data.workers : [];
    renderHeader(workers);
    const sig = JSON.stringify(workers);
    if (sig !== workerSig) {
      workerSig = sig;
      renderRows(workers);
    }
    renderCharts();
  };

  const noteFlags = (workers) => {
    const now = Date.now();
    const present = new Set();
    workers.forEach((w) => {
      const id = String(w.id || '');
      if (!id) return;
      present.add(id);
      const flag = flagOf(w);
      if (flag && flag !== flagSeen.get(id)) flagMarks.push({ t: now, flag: flag });
      if (flag) flagSeen.set(id, flag);
      else flagSeen.delete(id);
    });
    flagSeen.forEach((_, id) => {
      if (!present.has(id)) flagSeen.delete(id);
    });
    const cutoff = now - WINDOW_MS;
    flagMarks = flagMarks.filter((m) => m.t >= cutoff);
  };

  const accept = (payload) => {
    if (!payload || typeof payload !== 'object') return;
    data = payload;
    const serverNow = parseTime(payload.now);
    skewMs = serverNow === null ? 0 : serverNow - Date.now();
    setUnreachable(false);
    noteFlags(Array.isArray(payload.workers) ? payload.workers : []);
    render();
  };

  const poll = () => {
    clearTimeout(pollTimer);
    pollTimer = setTimeout(poll, POLL_MS);
    if (request) request.abort();
    const controller = new AbortController();
    request = controller;
    fetch('/api/dashboard', { signal: controller.signal, cache: 'no-store' })
      .then((res) => {
        if (!res.ok) throw new Error('http ' + res.status);
        return res.json();
      })
      .then((payload) => {
        if (request === controller) request = null;
        accept(payload);
      })
      .catch((err) => {
        if (err && err.name === 'AbortError') return;
        if (request === controller) request = null;
        setUnreachable(true);
      });
  };

  const tick = () => {
    dom.clock.textContent = new Date().toTimeString().slice(0, 8);
    const now = Date.now() + skewMs;
    tracks.forEach((t) => {
      if (t.live) paintTrack(t, now);
    });
  };

  fit();
  setTimeout(fit, 300);
  window.addEventListener('resize', fit);
  tick();
  setInterval(tick, 1000);
  poll();
})();
